akka {
  remote {
    artery {
      transport = tcp
      canonical.hostname = "127.0.0.1"
      canonical.port = 2553
    }
  }
  loglevel = "INFO"
  actor.debug {
    receive = on
    lifecycle = on
  }
}
